import 'package:flutter/material.dart';

class NohaKhanScreenNew extends StatelessWidget {
  const NohaKhanScreenNew({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Noha Khan')),
      body: const Center(child: Text('Noha Khan Screen')),
    );
  }
}
