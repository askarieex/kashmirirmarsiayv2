import 'package:flutter/material.dart';

class ZakirScreen extends StatelessWidget {
  const ZakirScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Zakir')),
      body: const Center(child: Text('Zakir Screen')),
    );
  }
}
